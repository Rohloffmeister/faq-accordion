const faq_titles = document.querySelectorAll('.faq-title');

/*
faq_titles.forEach(title => {
  title.addEventListener('click', () => {
    console.log("test");
    switch (title.getElementsByTagName('input')[0].checked) {
        case true:
            title.getElementsByTagName('input')[0].checked = false;
            
            break;
        case false:
            title.getElementsByTagName('input')[0].checked = true;
            break;
    
        default:
            break;
    }
})});
*/